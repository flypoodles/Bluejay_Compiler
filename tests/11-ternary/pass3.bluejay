fun recursive void (i int)
  var n int := i-1;

  if (i == 0)  return;
  printint(i);
  printstring(" ");
  printstring((i%2) ? "odd" : "even");
  printstring(" ");
  recursive(n);


recursive(10);
return 0;
